/**
 * Offres donnant droit à l'export CSV.
 *
 * Aligné sur ce qu'annonce la grille tarifaire : l'export est une contrepartie
 * du passage à une offre payante. Le plan à vie y a droit au même titre que
 * les abonnements — c'est une offre payante, pas une version gratuite.
 */
export const EXPORT_PLANS = ["starter", "scale", "lifetime", "pro"];
